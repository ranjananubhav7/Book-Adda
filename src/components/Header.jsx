import React, {useState} from "react";

var Header = () => {
return <div>
    <h1 className="headerText">BookAdda</h1>
    <button className="headerButton">sign up</button>
    <button className="headerButton">login</button>
</div>
}

export default Header;